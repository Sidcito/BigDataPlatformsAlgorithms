The Epinions social network dataset needs to be downloaded and and placed in the Hadoop File System\
A directory needs to be created which would contain the output\
A JAR needs to be created and named as page rank.jar following which the command of << hadoop jar page rank.jar - - input <in> - - output <out> >> is used to launch\
\
-h can be used to display the help text\
Damping is by default set to 0.85 but can vary from 0 to 0.85\
Count is by default set to 2}
